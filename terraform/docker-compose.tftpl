version: '3'
services:
  app:
    image: cr.yandex/${image_path}:test2
    ports:
      - "80:8000"
    environment:
      - DATABASE_URL=postgresql://${db_user}:${db_password}@${db_host}:6432/${db_name}
      - ADMINS_ID={admins_id}
    restart: always
