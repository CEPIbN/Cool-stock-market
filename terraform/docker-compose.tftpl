
version: '3'
services:
  app:
    image: cr.yandex/your-registry-id/your-image-name:latest
    environment:
      - DATABASE_URL=postgresql://${db_user}:${db_password}@${db_host}:6432/${db_name}
    restart: always
